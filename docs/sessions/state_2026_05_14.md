# Profit Sentinel — Claude Code Build State
Date: 2026-05-14
Instructions: Read CLAUDE.md first, then this file, before 
writing any code. Confirm current state before beginning.

---

## Current Build Step

**Step 3 — Connect remaining sources via Airbyte**
Status: In progress

Sub-steps completed this session:
- 3b. Klaviyo — COMPLETE (verified below)
- 3d. Loop Returns — COMPLETE, placeholder schema (verified below)
- 3e. GA4 — COMPLETE (verified below)

Sub-steps not yet started:
- 3a. Meta Ads
- 3c. Gorgias
- 3f. Sentry schema (manual)
- 3g. TikTok schema (manual)
- 3h/3i. schema_discovery + python_transformer for 3a, 3c, 3f-3g

---

## What Was Built in Previous Sessions

See docs/sessions/build_state_2026_05_13.md for full 
Step 1 and Step 2 detail. Summary:

- public.client_config (135 cols, 2 triggers)
- public.alert_log, config_change_log, thread_context
- public.source_schema_registry (RLS enabled)
- public.schema_versions (RLS enabled)
- connectors/schema_discovery.py
- connectors/python_transformer.py (incremental load)
- client_azure_co: 47 Shopify tables + stg_shopify_orders

---

## What Was Built This Session

### Step 3b — Klaviyo (COMPLETE)

**Tables processed:** 6
**Script used:** connectors/_run_klaviyo.py (orchestrator)

#### Step 1 — is_synthetic column

All 6 tables confirmed present:

  klaviyo_campaigns    is_synthetic: present
  klaviyo_events       is_synthetic: present
  klaviyo_flows        is_synthetic: present
  klaviyo_lists        is_synthetic: present
  klaviyo_metrics      is_synthetic: present
  klaviyo_profiles     is_synthetic: present

#### Step 2 — Schema discovery results

  klaviyo_campaigns    11 columns registered
  klaviyo_events       11 columns registered
  klaviyo_flows        11 columns registered
  klaviyo_lists        11 columns registered
  klaviyo_metrics      11 columns registered
  klaviyo_profiles     12 columns registered

Total Klaviyo columns in source_schema_registry: 67
No errors. No type changes (first registration).

#### Step 3 — First load row counts

  klaviyo_campaigns     0 rows (table empty in Airbyte sync)
  klaviyo_events        1 row
  klaviyo_flows         1 row
  klaviyo_lists         3 rows
  klaviyo_metrics      20 rows
  klaviyo_profiles      1 row

Note: klaviyo_campaigns has 0 rows in both raw and staging.
This is correct — no campaigns have been sent from the 
dev Klaviyo account yet. Not a pipeline failure.

#### Step 4 — Two-run incremental test

All 6 tables: PASS
  mode        = incremental (run 2)
  rows        = 0 (correct — no new Airbyte sync between runs)
  wm_col      = _airbyte_extracted_at (Destinations V2)

#### Step 5 — Row count verification

  Table                   Raw    Staging   Result
  klaviyo_campaigns         0          0   MATCH
  klaviyo_events            1          1   MATCH
  klaviyo_flows             1          1   MATCH
  klaviyo_lists             3          3   MATCH
  klaviyo_metrics          20         20   MATCH
  klaviyo_profiles          1          1   MATCH

#### Summary table

  Table                Cols   Raw   Stg   Counts  2-run  Fallbacks
  klaviyo_campaigns      11     0     0    MATCH   PASS   0
  klaviyo_events         11     1     1    MATCH   PASS   0
  klaviyo_flows          11     1     1    MATCH   PASS   0
  klaviyo_lists          11     3     3    MATCH   PASS   0
  klaviyo_metrics        11    20    20    MATCH   PASS   0
  klaviyo_profiles       12     1     1    MATCH   PASS   0

Overall: ALL PASS

#### Issues encountered

One warning logged before Step 1 ran:
  "Staging table stg_klaviyo_campaigns exists but is empty 
   — performing full load."
Cause: a partial staging table existed from a prior manual 
run before this session. Behaviour was correct — full load 
executed, row counts matched (0 = 0). Not a failure.

#### Reusability note

connectors/_run_klaviyo.py orchestrator pattern is directly 
reusable for Gorgias and Meta once those Airbyte syncs are 
triggered. Swap TABLES list and SOURCE_NAME only — all 
pipeline logic (is_synthetic, discovery, transform, 
incremental test, row count verification) is source-agnostic.

---

### Step 3d — Loop Returns (COMPLETE — placeholder schema)

**Schema type:** Manually designed (not from real Airbyte sync)
**Schema file:** docs/manual_schemas/loop_returns_schema.sql
**Script used:** connectors/_run_loop_returns.py

#### Tables created in client_azure_co

  loop_returns             (13 columns)
  loop_return_line_items   (13 columns)
  loop_refunds             ( 9 columns)

#### Schema discovery results

  loop_returns             13 columns registered, 0 errors
  loop_return_line_items   13 columns registered, 0 errors
  loop_refunds              9 columns registered, 0 errors

  Total loop_returns columns in source_schema_registry: 35

#### Staging tables created

  client_azure_co.stg_loop_returns             — created
  client_azure_co.stg_loop_return_line_items   — created
  client_azure_co.stg_loop_refunds             — created

All 3 staged 0 rows (correct — tables are empty placeholders;
synthetic data seeded in Step 5 of build sequence).

#### Two-run incremental test

  Table                        2-run   wm_col
  loop_returns                  PASS   _airbyte_extracted_at
  loop_return_line_items        PASS   _airbyte_extracted_at
  loop_refunds                  PASS   _airbyte_extracted_at

#### Summary table

  Table                  Cols   Stg?  2-run  Fallbacks
  loop_returns             13    yes   PASS   0
  loop_return_line_items   13    yes   PASS   0
  loop_refunds              9    yes   PASS   0

  Overall: ALL PASS

#### Safe for / Must re-verify before

Safe for:
  - Synthetic data (Step 5 seed script)
  - dbt staging and mart models built on synthetic data
  - Agent A/B/C/D testing against synthetic data

Must re-verify before:
  - Connecting a real client Loop Returns account
  - Treating dbt output as production-accurate
  - Activating Loop signals in Agent A for live client

Re-verification process when real client onboards:
  1. Connect Loop Returns via Airbyte → client schema
  2. Run schema_discovery.py against real Airbyte tables
  3. Compare real column names against placeholder schema
  4. If names differ: update dbt models and agent logic
  5. Drop placeholder tables once real tables confirmed

#### CONDITIONAL activation

Agents do not activate Loop signals until customer 
discovery confirms >5 of first 10 interviewed brands 
use Loop Returns. If condition not met: tables remain 
in schema, synthetic data is seeded, but agent signal 
library does not include Loop signals.

---

### Step 3e — GA4 (COMPLETE)

**Tables processed:** 6 (discovered dynamically from information_schema)
**Script used:** connectors/_run_ga4.py (orchestrator)

#### Step 1 — GA4 tables discovered

  ga4_conversions_report
  ga4_daily_active_users
  ga4_devices
  ga4_events_report
  ga4_pages
  ga4_traffic_acquisition_session_default_channel_grouping_report

  Total: 6 tables found

#### Step 2 — is_synthetic column

All 6 tables confirmed present.

#### Step 3 — Schema discovery results

  ga4_conversions_report                         12 columns registered
  ga4_daily_active_users                         10 columns registered
  ga4_devices                                    20 columns registered
  ga4_events_report                              14 columns registered
  ga4_pages                                      13 columns registered
  ga4_traffic_acquisition_...grouping_report     18 columns registered

  Total GA4 columns in source_schema_registry: 87
  No errors. No type changes (first registration).

#### Step 4 — First load row counts

All 6 tables: 0 rows.
Note: GA4 dev property has no historical data in Airbyte yet.
0 rows is correct — not a pipeline failure.

#### Step 5 — Two-run incremental test

All 6 tables: PASS
  mode   = incremental (run 2)
  rows   = 0 (correct — no new Airbyte sync between runs)
  wm_col = _airbyte_extracted_at (Destinations V2)

#### Step 6 — Row count verification

All 6 tables: raw=0, staging=0, MATCH

#### Summary table

  Table                                          Cols   Raw   Stg   Counts  2-run  Fallbacks
  ga4_conversions_report                           12     0     0    MATCH   PASS   0
  ga4_daily_active_users                           10     0     0    MATCH   PASS   0
  ga4_devices                                      20     0     0    MATCH   PASS   0
  ga4_events_report                                14     0     0    MATCH   PASS   0
  ga4_pages                                        13     0     0    MATCH   PASS   0
  ga4_traffic_acquisition_...grouping_report       18     0     0    MATCH   PASS   0

  Overall: ALL PASS

#### Issues encountered

Six warnings logged before Step 4 ran:
  "Staging table stg_ga4_* exists but is empty — performing full load."
Cause: partial staging tables existed from a prior manual run before
this session. Behaviour was correct — full load executed, row counts
matched (0 = 0). Not a failure. Same pattern as Klaviyo session.

#### Key design note

connectors/_run_ga4.py discovers GA4 tables dynamically from
information_schema at runtime (LIKE 'ga4\_%'). This handles any
number of GA4 streams without code changes — new Airbyte streams
are automatically included on next run.

---

## Known Debt (carry-forward from build_state_2026_05_13.md)

**DEBT-001 — Duties-set columns incorrectly typed**
  current_total_duties_set and original_total_duties_set 
  in shopify_orders registered as cast_text_to_numeric.
  Correct type: jsonb_extract_from_text.
  Fix: add 'duties_set' to _JSONB_TEXT_PATTERNS before 
  any international client onboards.

**DEBT-002 — is_synthetic column**
  Shopify source tables (47) still missing is_synthetic.
  Klaviyo tables now have it.
  Fix required: Step 4 (ALTER TABLE for Shopify tables).

**DEBT-003 — dbt staging models point at wrong schema**
  Do not run dbt until Step 6.

**DEBT-004 — Sync validation script not yet built**
  Build connectors/validate_sync.py after all Step 3 
  sources are complete. Script must:
  - Compare API record counts vs Supabase staging counts
    for every source and stream
  - Write results to public.sync_validation_log
    (timestamp, source, stream, api_count,
    supabase_count, variance_pct, pass_fail)
  - Run after every Airbyte sync, before dbt runs
  - Suppress Agent A alerts if recent sync failed validation
  - Tolerance threshold: >1% variance = fail
  Sources to cover when built:
    Shopify, Klaviyo, Gorgias, Loop Returns,
    Meta, TikTok, GA4, Sentry
  Do not build until all Step 3 sources are connected 
  or schema-designed manually.

---

## source_schema_registry state as of this session

  Source         Columns registered
  shopify        101  (shopify_orders only — other tables pending)
  klaviyo         67  (all 6 tables)
  loop_returns    35  (3 placeholder tables)
  ga4             87  (all 6 tables)
  Total:         290

---

## Step 3 remaining sub-steps

3a. Meta Ads via Airbyte Cloud UI
    → destination schema: client_azure_co
    → trigger first sync
    → confirm tables land in client_azure_co
    NOTE: Read Section 8 of technical_architecture.md 
    (Meta Ads API Breaking Changes May 2026) before 
    configuring connector. Verify Facebook Marketing 
    connector is v22.0+ before first sync.

3c. Gorgias via Airbyte Cloud UI
    → destination schema: client_azure_co
    → trigger first sync

3d. Loop Returns via Airbyte Cloud UI
    → CONDITIONAL: only if >5 of first 10 interviewed 
      brands confirmed Loop Returns users.

3e. GA4 — COMPLETE (see above)

3f. Sentry table schema — manual design
    → table: sentry_errors_daily
    → create in client_azure_co

3g. TikTok table schema — manual design
    → table: tiktok_ad_performance
    → create in client_azure_co

3h. Run schema_discovery.py against all new tables
3i. Run python_transformer.py against all new tables

Step 3 complete when all source tables have staging 
tables and source_schema_registry has entries for all 
columns across all sources.

---

## File Locations

Project root:
C:\Users\Anupam\OneDrive\Desktop\Profit Sentinel\
profit-sentinel-product\profit-sentinel\

Connectors:
[project root]\connectors\schema_discovery.py
[project root]\connectors\python_transformer.py
[project root]\connectors\_run_klaviyo.py  (orchestrator — reuse pattern for other sources)

Session docs:
[project root]\docs\sessions\build_state_2026_05_13.md  (Steps 1-2)
[project root]\docs\sessions\state_2026_05_14.md        (Step 3, this file)

---

## Rules Reminder (from CLAUDE.md)

- RULE 1: information_schema discovery before transformation
- RULE 2: All queries schema-qualified
- RULE 3: Synthetic data toggle guard (is_synthetic column 
  now present on Klaviyo tables; still missing on Shopify)
- RULE 4: Zero casts in dbt
- RULE 5: Structured error logging with SOURCE field
- RULE 6: Pre-completion review — NULL, schema drift, rate limits
- RULE 7: Agent A never calls Claude API
- RULE 8: RLS on public tables (done)
- RULE 9: Update technical_architecture.md after step completes
