# Profit Sentinel — Session State
## Date: 2026-05-20 (v2)
## Session: historical_pattern_scan.py Design — Q4–Q6 Locked. All 6 questions resolved.

---

## LAST COMPLETED BUILD STEP

Step 10.5 — Mart patch (13 columns) + vip_purchase_gap_days fix.
PASS=22 WARN=0 ERROR=0.
Commit: see state_2026_05_19_session4.md for full detail.

---

## WHAT WAS RESOLVED THIS SESSION (Claude.ai strategy — no Claude Code)

### Q4 — `client_specific` Flag Promotion Rules (LOCKED)

| Decision | Resolution |
|----------|-----------|
| Scope | Novel chains only — 56 hardcoded chains not subject to promotion logic |
| Track 1 global promotion | Deferred to post-10-client milestone — logged as DEBT-T1 (D-19) |
| Track 2 single-client | `practitioner_approved` required, `client_specific = true` permanently |
| `client_specific` flip timing | Set to `false` on `causal_pattern_validation` only after re-scan confirms — not at practitioner approval |
| Monthly auto-check | Post-sweep: `cross_client_instance_count ≥ 3` AND `calendar_clustered = false` → `promotion_status = 'validated'` → practitioner review queue |
| Pair matching | Exact string match on `leading_signal_column` + `outcome_column` — no alias resolution |
| Vertical scope | Novel chains vertical-specific in both tracks — no cross-vertical promotion ever |
| Multi-vertical clients | Separate `causal_pattern_validation` rows per `vertical_tag` — CD-4 hard dependency |

---

### Q5 — Failure Handling: Insufficient History (LOCKED)

| Decision | Resolution |
|----------|-----------|
| 5A: Novel vs hardcoded code paths | Completely separate — no merging at any stage. Novel sweep → `candidate_signals` only. Hardcoded validation → `causal_pattern_validation` only. Post-Track-1-promotion: novel chains hardcoded and written to `causal_pattern_validation` — indistinguishable from original 56 in operation. |
| 5B: All-source DQ pre-checks | Every source gets DQ pre-check at onboarding scan, written to `dq_metric_scores`. Gorgias checks: tag coverage rate (<50% → skip C1 chains), tag vocabulary size, compound tag presence, return/sizing tag rate vs Loop return rate, agent tagging variance. Other sources: Meta (% unmatched spend), TikTok (creator-to-SKU mapping), Loop (reason code coverage), Klaviyo (active post-purchase flow), GA4 (funnel step completeness), Sentry (coverage window). NLP classifier for Gorgias ticket bodies: discovery-gated (D-22) — build only after discovery confirms Alert 5 demand and tagging inconsistency across 3+ beta clients. When built: covers all return reasons, not sizing only. Full pipeline, not lightweight. |
| 5C: Missing connector | `scan_skipped_reason text` column on `causal_pattern_validation` — dedicated column, not jsonb. Values: 'connector_missing' / 'insufficient_history' / 'below_sparsity_threshold' / 'gorgias_tagging_insufficient' / 'connector_name:{name}'. Monthly incremental scan auto-detects when previously-missing connector now present → re-runs skipped chains → internal notification only. |
| Late connector addition | `pending_connectors text[]` added to `client_config`. Populated at onboarding confirmation flow (new question at end of existing 5 questions: "Are you planning to add any sources in the next 90 days?"). Monthly scan checks flag and re-triggers on connection. |

---

### Q6 — Output Format (LOCKED)

| Decision | Resolution |
|----------|-----------|
| Headline lookback | Deepest single-source (Shopify/Klaviyo anchor). Per-source limits handled silently — short-history connectors don't reduce headline number. |
| $ leakage display threshold | ≥1% of derived annual GMV AND ≥2 distinct patterns. Below threshold: suppress $ section entirely, no explanation. |
| GMV derivation | From Shopify total order revenue in scan window — not founder-stated. Written to `client_config.gmv_derived_annual`. Updated on every monthly incremental scan. |
| Onboarding message: two variants | Variant 1 ($ threshold crossed): leakage section with specific patterns and $ figures, then forward promise. Variant 2 (not crossed): straight to forward promise, no mention of suppressed section. |
| NLQ reference | Included in both variants. "Why did my ROAS drop?" is accurate — Agent B handles causal reasoning at pilot. |
| Unknown pattern handling | "For patterns I haven't seen before in your data, I'll tell you what I see and flag that I'm still learning the cause — I get sharper the longer I run." |
| Agree/Snooze/Disagree framing | Included — explained as personalisation mechanism, not just feedback. |
| "Your first alert will fire" | Only if ≥1 chain at provisional or core tier. Softened to "alerts will fire as patterns develop" if all chains are candidate only. |

**Onboarding completion message — both variants documented in product_strategy.md Section 5.**

---

## DOCUMENTATION CHANGES THIS SESSION

| File | Action | Changes |
|------|--------|---------|
| `technical_architecture.md` | REPLACE | Promotion rules rewritten (Track 1 deferred, pair matching, client_specific flip timing, monthly auto-check, vertical scope, multi-vertical clients). Novel chain section: separate code path stated, per-source DQ pre-check table added. `causal_pattern_validation`: `scan_skipped_reason` added. `client_config`: `pending_connectors`, `pending_connectors_noted_at`, `gmv_derived_annual`, `gmv_derived_at` added. `dq_metric_scores`: per-source DQ pre-check spec + NLP gating note appended. |
| `product_strategy.md` | REPLACE | Historical scan novel chain section updated (Track 1 deferred, separate code path, flip timing). Section 5: full onboarding completion message spec added — two variants, headline lookback rule, $ threshold rule, GMV derivation rule, implementation notes. |
| `pre_agent_build_checklist.md` | REPLACE | D-12 fully rewritten. D-17 updated. D-19 added (DEBT-T1). D-20 added (pending connectors). D-21 added (GMV derivation). D-22 added (Gorgias NLP classifier — discovery-gated). |
| `state_2026_05_20_v2.md` | ADD | This file. |

---

## NEW DEBT ITEMS THIS SESSION

| ID | Item | Trigger |
|----|------|---------|
| DEBT-T1 / D-19 | Track 1 cross-client convergence threshold (currently 3 clients) is provisional — re-evaluate at 10+ clients with real signal data | Post-10-client |
| D-20 | Pending connectors question in onboarding confirmation flow | Pre-first-client |
| D-21 | GMV derivation from Shopify at onboarding scan | Pre-first-client |
| D-22 | Gorgias NLP classifier — discovery-gated, full pipeline when built | Discovery-gated |

---

## PRE-AGENT B BLOCKERS STATUS

| Item | Status |
|------|--------|
| D-12: historical_pattern_scan.py | **ALL 6 QUESTIONS LOCKED. Ready for Claude Code prompt.** |
| B-1: causal_pattern_validation seed rows | PENDING |
| B-2: candidate_signals seed rows | PENDING |
| B-4: G1 ad set → SKU mapping decision | PENDING |
| B-5: Fashion Causal Graph in code | PENDING |
| B-6: promotion_threshold values | PENDING |
| D-17: Novel chain review infrastructure | PENDING — schema changes needed in DB before Agent B build |
| D-20: pending_connectors onboarding question | PENDING |
| D-21: GMV derivation at onboarding scan | PENDING |
| S3-P3: stg_klaviyo_profiles column names | PENDING — before Agent B build |

---

## NEXT ACTION

**Write Claude Code prompt for historical_pattern_scan.py.**
All 6 design questions resolved and locked. Claude Code prompt is the immediate next step.

---

## BUILD SEQUENCE STATUS

| Step | Description | Status |
|------|-------------|--------|
| 1 | Environment setup | Complete ✓ |
| 2 | Airbyte connectors | Complete ✓ |
| 3 | Source schema registry (72 tables) | Complete ✓ |
| 4 | Staging tables | Complete ✓ |
| 5a–5h | All seed scripts | Complete ✓ |
| 5i | patch_script_final.py | Complete ✓ |
| 6 | dbt rebuild (22 models) | Complete ✓ |
| 7 | Validation (37 alerts) | Complete ✓ |
| 8 | Onboarding confirmation flow CLI | Complete ✓ |
| 9 | Agent A (LangGraph) | Complete ✓ |
| 10 | Slack delivery + Evidence Stack | Complete ✓ |
| 10.5 | Mart patch (13 columns) + vip fix | Complete ✓ |
| 11 | Agent B (Causal graph traversal) | Pending — pre-Agent B gaps must clear first |
| 12 | Agent C (Recommendation engine) | Pending |
| 13 | Agent D (Evidence Stack formatter) | Pending |

---

## KNOWN SCHEMA DRIFT — CARRY FORWARD

- `alert_log` column: `alert_type` (NOT `signal_type`)
- `alert_log` column: `evidence_stack_json` (NOT `evidence_stack`)
- `signal_value` + `threshold_value` are separate numerics (NOT `signal_values` jsonb)
- `client_id` throughout: `client_azure_co` (NOT `azure_co`)
- Airbyte drops columns not in schema on every sync → `is_synthetic` lives in staging tables (`stg_*`) for Airbyte sources
- Meta attribution window hard break: January 12 2026
- `brand_event_calendar` has zero rows in synthetic data
- `stg_klaviyo_profiles` columns: `profile_id` (not `customer_id`), `vip_status` (not `is_vip`)
- `stg_loop_refunds` not present — use `stg_loop_returns` for refund lag
- `stg_meta_ad_performance` has no `attributed_revenue` — proxy: spend × purchase_roas
- `stg_klaviyo_flows` has no date column — use `stg_klaviyo_email_events` for all time-series Klaviyo mart CTEs
- GA4 tables (`ga4_pages`, `ga4_devices`) absent in synthetic data — NULL mart columns expected until real client onboards
