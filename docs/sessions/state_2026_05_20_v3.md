# Profit Sentinel — Session State
## Date: 2026-05-20 (v3)
## Session: historical_pattern_scan.py Build + Execution — D-12 Complete

---

## LAST COMPLETED BUILD STEP

Step 11 — `historical_pattern_scan.py` built and executed in `--mode full`.
All 5 verification conditions passed at 13:09 UTC.

---

## WHAT WAS COMPLETED THIS SESSION

### historical_pattern_scan.py — Built and Executed (D-12 COMPLETE)

Script location: `connectors/historical_pattern_scan.py`

**Execution results:**

| # | Check | Result |
|---|-------|--------|
| 1 | `client_config.historical_scan_status` | `complete` ✓ (`historical_scan_completed = true`) |
| 2 | `causal_pattern_validation` rows | 22 rows ✓ — 1 core, 1 provisional, 20 candidate |
| 3 | `candidate_signals` (novel pairs) | 440 rows ✓ |
| 4 | `dq_metric_scores` (today's date) | 7 rows ✓ — all sources timestamped |
| 5 | `onboarding_messages` | 1 row ✓ — variant: `forward_promise` |

**Phase log summary:**
- Phase 1 (DQ): 7 sources checked, 2 chains pre-skipped
  - C1 skipped: `gorgias_tagging_insufficient`
  - Chain5 skipped: `insufficient_history`
- Phase 2 (Known chains): 14 chains scanned, 8 skipped — 1 core, 1 provisional, 20 candidate
- Phase 3 (Novel discovery): 500-pair cap applied, 440 pairs written, 80 flagged `single_client_core`
- Phase 4 (GMV): `gmv_derived_annual = $6,818,827.73`
- Phase 5 (Lookback): 718 mart days processed
- Phase 6 (Onboarding message): `forward_promise` variant written
- Phase 7 (Final status): complete

**Known deviations to carry forward:**
- `dq_metric_scores` uses `timestamptz` for date field (not plain `date`) —
  `WHERE score_date = CURRENT_DATE` queries must cast or use date_trunc
- 80 of 440 novel pairs flagged `single_client_core` on synthetic data — expected
  given 24 months of strongly correlated synthetic signals. Will be significantly
  lower on real client data. Do not treat this number as a production baseline.
- Onboarding message is `forward_promise` (not `leakage`) — leakage threshold
  (≥1% of GMV with ≥2 provisional/core chains) not crossed on synthetic data.
  Expected. Leakage variant will fire on real client data with richer history.
- One bug auto-corrected by Claude Code during execution: `RealDictCursor` in
  `run_dq_prechecks` passed to `_table_exists` which used `fetchone()[0]` —
  fixed to use column name key. No data impact.

---

## NEW TABLE CREATED THIS SESSION

`public.onboarding_messages` — created by `_ensure_tables()` in the script:
```sql
CREATE TABLE IF NOT EXISTS public.onboarding_messages (
    id              bigint generated always as identity primary key,
    client_id       text not null,
    message_variant text not null,  -- 'leakage' or 'forward_promise'
    message_text    text not null,
    generated_at    timestamptz default now(),
    sent            boolean default false
);
```

---

## ENVIRONMENT CONFIGURATION NOTE

`CLAUDE_CODE_MAX_OUTPUT_TOKENS = 60000` added as permanent Windows system
environment variable. Required because the script generation exceeded the
default 32,000 token limit. Set before running any future large code
generation sessions in Claude Desktop.

---

## PRE-AGENT B BLOCKERS STATUS (UPDATED)

| Item | Status |
|------|--------|
| D-12: historical_pattern_scan.py | **COMPLETE ✓** — All 5 checks pass |
| B-1: causal_pattern_validation seed rows (56 hardcoded chains) | PENDING |
| B-2: candidate_signals seed rows | PENDING |
| B-4: G1 ad set → SKU mapping decision | PENDING |
| B-5: Fashion Causal Graph in code | PENDING |
| B-6: promotion_threshold values | PENDING |
| D-17: Novel chain review infrastructure | PENDING |
| D-20: pending_connectors onboarding question | PENDING |
| D-21: GMV derivation at onboarding scan | **COMPLETE ✓** — implemented in Phase 4 of script |
| S3-P3: stg_klaviyo_profiles column names | PENDING — before Agent B build |

**Note:** D-21 is now complete — GMV derivation implemented in
`historical_pattern_scan.py` Phase 4 and writes to `client_config`.

---

## NEXT ACTION

Next pre-Agent B blocker: **B-1** — seed the 56 hardcoded chains into
`causal_pattern_validation`. This requires a design session to confirm:
1. Which of the 56 chains are distinct from the 22 already written by
   `historical_pattern_scan.py` (avoid duplicates on `causal_chain_id + vertical_tag`)
2. What seed values to use for `instance_count`, `confidence_tier`, and
   `threshold_at_scan_time` for the hardcoded chains not yet scanned

**B-5 (Fashion Causal Graph in code)** is the other high-priority item —
Agent B cannot traverse the graph without a Python/JSON representation.
Consider whether B-1 and B-5 should be designed together.

---

## BUILD SEQUENCE STATUS

| Step | Description | Status |
|------|-------------|--------|
| 1 | Environment setup | Complete ✓ |
| 2 | Airbyte connectors | Complete ✓ |
| 3 | Source schema registry (72 tables) | Complete ✓ |
| 4 | Staging tables | Complete ✓ |
| 5a–5h | All seed scripts | Complete ✓ |
| 5i | patch_script_final.py | Complete ✓ |
| 6 | dbt rebuild (22 models) | Complete ✓ |
| 7 | Validation (37 alerts) | Complete ✓ |
| 8 | Onboarding confirmation flow CLI | Complete ✓ |
| 9 | Agent A (LangGraph) | Complete ✓ |
| 10 | Slack delivery + Evidence Stack | Complete ✓ |
| 10.5 | Mart patch (13 columns) + vip fix | Complete ✓ |
| 11 | historical_pattern_scan.py | **Complete ✓** |
| 12 | Agent B (Causal graph traversal) | Pending — pre-Agent B gaps must clear first |
| 13 | Agent C (Recommendation engine) | Pending |
| 14 | Agent D (Evidence Stack formatter) | Pending |

---

## KNOWN SCHEMA DRIFT — CARRY FORWARD

- `alert_log` column: `alert_type` (NOT `signal_type`)
- `alert_log` column: `evidence_stack_json` (NOT `evidence_stack`)
- `signal_value` + `threshold_value` are separate numerics (NOT `signal_values` jsonb)
- `client_id` throughout: `client_azure_co` (NOT `azure_co`)
- Airbyte drops columns not in schema on every sync → `is_synthetic` lives in
  staging tables (`stg_*`) for Airbyte sources
- Meta attribution window hard break: January 12 2026
- `brand_event_calendar` has zero rows in synthetic data
- `stg_klaviyo_profiles` columns: `profile_id` (not `customer_id`),
  `vip_status` (not `is_vip`)
- `stg_loop_refunds` not present — use `stg_loop_returns` for refund lag
- `stg_meta_ad_performance` has no `attributed_revenue` — proxy: spend × purchase_roas
- `stg_klaviyo_flows` has no date column — use `stg_klaviyo_email_events`
  for all time-series Klaviyo mart CTEs
- GA4 tables (`ga4_pages`, `ga4_devices`) absent in synthetic data — NULL
  mart columns expected until real client onboards
- `dq_metric_scores.score_date` stored as `timestamptz` not `date` — use
  `date_trunc('day', score_date) = CURRENT_DATE` in queries
