# Pilot Readiness Register

**Purpose:** The single durable tracker of everything that must happen before the DTC Prism pilot goes live — the work that is *not* a documentation fix. Documentation reconciliation (the doc-sync) is complete; this file carries the remaining build, decision, and first-connect work.

**Status:** Created 2026-07-02 at the close of the canonical-doc reconciliation (doc-sync). Five canonical files edited/committed (`pilot_scope` 33d337f, `product_strategy` d695453, `cross_alert_orchestration` 09834fd, `technical_architecture` 5a6bd3d); four verified no-edit/clean (`pre_agent_build_checklist`, `d1_validation_gates`, `agent_d_build_spec`, plus `save_protocol` + `CLAUDE.md` clean by nature).

**2026-07-27 database verification pass:** eight database-state claims behind register items were verified against the live database before being written here. Three closed outright (CD-6, CD-7, CD-9), one dropped (D-6), three stood (CD-5, D-1, D-10), and six findings surfaced that were on no list. Items resting on that pass carry *(checked-against: live DB query · 2026-07-27)*. Code-state claims were deliberately NOT checked and are tagged UNVERIFIED — database state drifts silently, code state does not, because every commit is founder-authorised.

**This is a tracking artifact, not a canonical spec.** It records *what work remains*, not *what the product is*. No line-count handle, no full save-protocol — it is maintained like the state files in `docs/sessions/`.

**Line-citation caveat:** file:line pointers below were captured during the doc-sync's collection phase. `product_strategy.md` citations predate the doc-sync edits and are shifted by roughly **+12 lines** in the current committed file (the C8 §3D block, the C8 summary row, and the §5 lead paragraph all added lines above ~:1365). `cross_alert_orchestration.md:650` is accurate (that file had count-only edits, no line shift). Verify against the current committed file before relying on any `ps:` line number.

**Freshness tags:** items whose status depends on another file's content carry an inline *(checked-against: <file> · <date>)* tag. Today's sessions found two register notes stale against the files they summarised; a date marker would have caught both.

---

## THE BINDING CONSTRAINT (read first)

Recruiting **4–5 design partners** is the true launch gate (pilot_scope.md §8). All architectural and build work below has no value until real brand data connects. Design-partner recruitment runs as an independent parallel track from day one. Everything in this register serves getting a real brand connected and the pilot alerts firing correctly on their data.

---

## OPEN QUESTIONS (undecided — do not close without evidence)

- **OQ-1 — C8 abnormality rule + series-fit.** The METHOD is fully specified; only the NUMBERS are open — this is a four-dial calibration job, not an unstarted design problem. Series-fit (C8 in Group C) is an inference — revisit if C8 turns out to be attribution-driven rather than returns-driven. *(Refs: buildstate_c8:103-104; c3-foundation:240-241.)* **No canonical home — this register is its only home.** The C8 abnormality rule is the SHARED detection rule for C3 — one method serves both (see BT-12 and pilot_scope §4 C3).
  - DECIDED (agreed 2026-06-10 in state_2026_06_10_c3_foundation_abnormal_bigenough_new.md, tagged provisional-pending-grouping; the grouping question that gated them CLOSED 2026-06-19 in state_2026-06-19_op1-close-last.md): ABNORMAL = the group's return rate sits clearly above ITS OWN robust historical band, built from middle-of-range percentiles, explicitly NOT mean/standard-deviation. TRUST GATE = a confidence band around the observed return rate; fires only when the PESSIMISTIC end still sits above normal — one test does abnormality, data-sufficiency and maturity at once; thin data widens the band and fails on its own; an absolute hard minimum sits beneath it so one expensive return never fires alone; sales value NEVER waives the trust gate. MATERIALITY = either return VOLUME or returned-order SALES VALUE crosses a brand-relative bar, sitting BEHIND the trust gate; size alone never fires. SIZE is on SALES VALUE, NOT cost — no cost dependency now or later for this alert. TRAJECTORY = a cohort return-RATE curve — is this order-cohort's cumulative return rate still climbing deep into the window when it should be tapering; NEVER an absolute count for the shape, because absolute counts are polluted by promotion timing; sits BEHIND the absolute size gate, cannot fire alone. NEW PRODUCTS with no history: silent watch, no alert, until a history-free signal crosses the size bar, then an honestly-worded size/direction alert ("new, no normal yet"). CONCENTRATION INSIDE THE GROUP is load-bearing — categories are lopsided, so a group average can look fine while one or two products drive the returns.
  - STILL OPEN — four dials, all calibration not design: (1) which percentiles define the robust band; (2) the confidence-band width (a strict pilot-start dial); (3) the absolute hard minimum beneath the trust gate; (4) where the brand-relative materiality bar sits — exactly what real brand data settles. PROPOSE now on test data, CONFIRM at first connect.
  - RETIRED, do not resurrect: separate period bands for sale vs non-sale; comparing a sale to prior comparable sales; cross-sectional or leave-one-out comparison against other categories.
  - CONFIRMATION PASS OWED: the 2026-06-10 decisions were tagged "provisional pending the grouping question"; that question closed 2026-06-19 but nothing went back to un-provisionalise them.
- **OQ-2 — Blended-marketing coverage-disclosure design.** The rule is locked (every channel the brand runs is named; unconnected ≠ zero; coverage disclosed). The *design* of how disclosure is surfaced to the founder is open. *(Point 3D.)* **No canonical home.**
- **OQ-3 — Weekly-digest cadence.** Weekly digest confirmed as pilot scope. The cadence-gating design (when/how it fires) is open, and there's a canonical-vs-Horizon-2 framing to resolve. *(prepilot-hardening:167-170.)*
- **OQ-4 — E5/E6 canon reconciliation.** E5/E6 exist in seed files but §3D lists only E1–E4; reconcile which is canonical. *(ps:~1377 post-edit.)*
- **OQ-5 — 3-namespace alert-numbering collision.** Three unrelated things share "A1/A2/…" labels: §3D alert IDs, `gap_abc_decisions.md` seed-design labels, and `seed_decisions` S1–S50/E-series. A future "check A6" is ambiguous across all three. **Canonical analysis lives at `cross_alert_orchestration.md:650` (P2-FINDING 5)**, which documents all three namespaces and recommends a naming convention (ALERT-A6 vs DEC-A6 vs S-rule). Confirmed still open (doc-integrity, not stale) — deliberately left untouched in the doc-sync. *(checked-against: cross_alert_orchestration.md · 2026-07-24)*
- **OQ-8 — Small causal-graph completeness.** Is each pilot symptom's root cause present in the causal graph before Agent B runs? **HIGHEST-RISK open item; no file mention anywhere — this register is its only home.**
- **OQ-9 — Delivery-loop mechanism.** The mechanism that gets an alert from the system to the founder is undesigned. **No canonical home.**
- **OQ-10 — Alert-count reconciliation.** Documents reconciled at 59 (2026-07-02 doc-sync, all current sites; the "58" in the product_strategy changelog is preserved history). Only the CODE (57) needs to catch up. (Delivery surface for pilot = email, decided.) *(checked-against: product_strategy.md · 2026-07-24)*
- **OQ-12 — Delivery-surface FINAL-PRODUCT decision (post-pilot).** Pilot uses EMAIL (confirmed, committed in pilot_scope). But `technical_architecture.md`'s full-product architecture is Slack-native: schema columns `slack_thread_ts`/`slack_channel` (NOT NULL), Slack Bolt data flow (:35), "founder never leaves Slack" surface (:48), Agent D posts to Slack (:814), Slack bot build steps. This was left untouched in the doc-sync (26 Slack refs intact) — it is NOT a find-replace. **After the pilot, decide on email's performance whether the final product stays email or moves to Slack.** If Slack: architecture already built. If email: the tech-arch delivery layer (schema + Agent D formatting + interaction model) needs a real redesign — schema-and-agent-deep, not a doc edit. Do not resolve until the pilot delivers evidence. *(Founder confirmed 2026-07-02: "trying email in pilot, decide email-vs-Slack for final product based on pilot experience.")* This is the delivery half of the former DH-1.
- **OQ-13 — Graduation rule (when an alert stops needing the human gate).** OPEN. The per-alert threshold that licenses removing the relevance gate (pilot_scope §2, column (b)) is undecided. The reviewer verdict MUST be LOGGED FROM THE FIRST ALERT even though the threshold is undecided — it cannot be reconstructed later. **No canonical home — this register is its only home.**
- **OQ-14 — Weekly-digest CONTENT (which numbers belong on the one page).** OPEN. OQ-3 covers digest CADENCE (when/how it fires); nothing covers CONTENT. The three items named in pilot_scope §4 (blended post-return ROAS, serial-offenders list, return-rate + reason table) are INDICATIVE, not final — the onboarding copy deliberately says "things like" for this reason. Settle before pilot launch. **No canonical home — this register is its only home.**
- **OQ-15 — G3 definition conflict. [PARKED 2026-07-27]** `product_strategy.md:588` says "G3 — Inventory Concentration Risk" (>40% of inventory value in one SKU family, Monitor-and-Wait, needs `sku_cost_master`); `agent_d_build_spec.md:331` says "G3 — Zero-Velocity SKU With Active Spend" (`deferred_phase2`). `pilot_scope.md` §4 park list says "G3 (COGS-gated)", matching the concentration version. Same code, two different alerts. HISTORY: a 2026-05-22 session redefined G3 to zero-velocity and explicitly dropped concentration to a Phase 2B candidate; it landed in agent_d on 2026-05-23 and propagated nowhere. This is NOT one file being newer — no June or July edit to either file touched G3, so file-level dates do not settle it. RECOMMENDATION: agent_d is right, §3D is stale; if confirmed, §3D is rewritten, pilot_scope's "COGS-gated" becomes spend-mapping-gated, and concentration is recorded as a Phase 2B candidate so it is not lost. PARKED at founder instruction — G3 is not a pilot alert under either definition. **No canonical home.**
- **OQ-16 — Creator profit truth: scope and claim boundary.** Whether C2 expands from "revenue after returns" to a fuller creator P&L: after returns, after the discount the code itself gave away, after removing orders the ad platforms also claimed, split new-vs-existing customer, against fee plus gifted-product cost. MUST include the explicit NOT-CLAIMED statement: whether the sale would have happened anyway (incrementality) needs holdout tests or media-mix modelling and is out of scope — writing the limit down is what stops this drifting into an attribution promise. EVIDENCE BAR: unprompted mentions across the ten discovery interviews (three probe questions sit in the India discovery questionnaire). NOT a pilot build item unless promoted; the double-count computation reuses the C8 order-level ad-platform join, so it is cheap AFTER C8 and expensive before. Competitive position corrected — see DH-2. **No canonical home.**

*Resolved & moved out: OQ-6 → BT-8; OQ-7 → BT-9; OQ-11 → "Checkout Error Spike" (Payment Gateway Failure = a cause node inside F2); delivery surface (pilot) → email.*

---

## BUILD TASKS (before a real brand connects, unless noted)

- **BT-1 — Blended-marketing math.** Add `google_spend` to `total_ad_spend` and all blended figures; build `stg_google_ads`, `google_roas`, `google_attributed_orders`; add the connected-channel registry + missing-vs-zero logic + coverage disclosure. Fixes A1/D1/A2/A5 at once.
- **BT-2 — C8 detection.** Add the C8 chain to `causal_graph.py` + wire Agent-A detection (`return_rate_pct` is not scanned today).
- **BT-3 — E5 chain + Agent B wiring.** Add the missing E5 chain to `causal_graph.py` (code 57→58); wire the orphaned causal_graph into Agent B (gated on E5).
- **BT-4 — Wire remaining pilot alerts into Agent A:** C6, G1, C2 (only C1 is scanned today; C8 wiring = BT-2). [AMENDED 2026-07-27] G1 wiring now covers the G1a/G1b split (BT-16) plus B-12's +/-1 day timing tolerance. BT-16 defines what fires; this item wires it.
- **BT-5 — [PARKED pre-pilot] Onboarding questionnaire.** COGS 3-option → 4-tier questionnaire remains parked. (ROAS opt-out field DROPPED per the display decision: ROAS always shown gross and net-of-returns, no founder toggle.) Pulled OUT of this parked item 2026-07-24: CD-10 per-client C1 calibration is a connect-day action (tracked at FC-3, not restated here); D-20 pending-connectors is PROMOTED to BT-14 below (vanish-risk — must not stay parked).
- **BT-6 — Debt / plumbing.** Parser per-brand accuracy gate (D-GAP6-21 / D1-G12); the `agent_a` hardcoded-55% margin (verify locus); GA4 `stg_ga4_pages`/`devices` (mart TODOs); Pass-Two SKU-contract validator (in R11).
- **BT-7 — TikTok catalog-report wiring** for C8 product-level spend + add an `is_synthetic` guard on the TikTok mart branch. *(mart:552-556.)*
- **BT-8 — [PILOT] Category cross-check.** Shopify-assigned category vs LLM category; flag disagreements (disagreement-handling is itself a design item). Pilot-critical — grouping correctness underpins C6/C8/C2.
- **BT-9 — Parser + labelling PROCESS finalized before pilot** (validation of the labels on real data is a first-connect action, FC-7).
- **BT-10 — A2 per-channel root-cause detection.** Build the four-cause decomposition (CPM inflation / creative fatigue / checkout errors / SKU return outlier) to run PER AD CHANNEL (Meta/Google/TikTok), with channel-attribution-confidence handling — name the channel it is confident about, flag ambiguity rather than forcing a single-channel verdict. *(Origin: A2 made multi-channel in the product_strategy §3D doc-sync commit; the §3D spec states multi-channel intent but detection is Meta-only in code.)*
- **BT-11 — C8 lineage note in code.** Add the A2→C8 retirement lineage note to A2's `causal_graph.py` entry (per buildstate_c8 2026-06-18). The product_strategy §3D C8 Note now points to `causal_graph.py` as the lineage home, so the code note must actually exist for that pointer to resolve. *(Origin: Flag-2 fix in the product_strategy doc-sync commit.)*
- **BT-12 — C3 detection wiring.** Per the 2026-06-03 resolution (`agent_d_build_spec.md:2510`): (a) wire C3 to the SAME per-category baseline D1 uses (S15) so C3 and D1 share one computation — C3's stated method does not reference S15 today (only D1 is wired to it, per :2500); and (b) decide C3's shared thin-history fallback: exposure test (D1's method, can still act) vs 90-day monitor-and-wait (C3's seeded method, waits) — a real design fork per :2506. The doc part (headline) is already done (product_strategy C3 "2×" neutralized); the S15 wiring + fallback decision are the build/design remainder. C-series detection, adjacent to BT-2 (C8). [DECIDED 2026-07-24: the thin-history fork resolves to ACT on the weaker signal with an explicit seasonality caveat, NOT 90-day monitor-and-wait — matches the documented narrate-don't-suppress posture. The shared-baseline (S15) wiring remains open.] *(checked-against: agent_d_build_spec.md · 2026-07-24)*
- **BT-13 — Human relevance gate + email delivery loop (ONE merged build: compose → hold → verdict → send).** Holding state (alert composed, not yet released); reviewer verdict field; three exits (send / reject-as-wrong / suppress-as-stale); one-day turnaround tracking; intraday FAST LANE for G1; a review-queue surface for Anupam; the founder action endpoint for Agree/Snooze/Disagree from email. CONSTRAINTS: (i) alert_log ALREADY carries the founder-outcome half (action_taken, action_taken_at, dismissal_reason, outcome_value_7d/14d) — build only the missing REVIEWER half; (ii) do NOT reuse the name "practitioner_approved" — that belongs to causal-chain promotion, a different mechanism. DESIGN NOTE: email security scanners and inbox prefetchers auto-click links, so the action link must land on a confirmation page with a button (two-step) and never write to the DB on the raw click. (Builds the delivery mechanism OQ-9 flags as undesigned.) ALL GENERATED OUTPUT: the gate and delivery loop cover EVERY generated founder-facing output, not just fired alerts and weekly digests — each is composed, held, reviewed and released through the SAME compose → hold → verdict → send path. This explicitly includes the onboarding completion message and the below-60%-confidence data-quality notification; neither is a bypass path. The onboarding completion message fires ONCE per brand and carries the highest-stakes numbers in the relationship — the first thing a design partner reads — so it must not auto-send. Not all outputs behave like alerts: neither a weekly digest nor the completion message has an Agree/Snooze/Disagree founder response or an intraday fast lane. [AMENDED 2026-07-27] (iii) `client_config` ALREADY carries `alert_approval_required` (boolean, default true, currently true) — check what it is wired to before building alongside it; it may be a Slack-era leftover or a live flag. Code state, UNVERIFIED. (iv) G1's hourly monitoring loop and its two-hour re-alert in `agent_d_build_spec` are generated founder-facing output and pass this SAME gate — there is no auto-send path. The spec's unqualified "re-alerts founder" needs removing or re-qualifying (A34).
- **BT-14 — [PROMOTED from BT-5, 2026-07-24] D-20 pending-connectors onboarding question.** "Are you planning to connect any additional sources in the next 90 days?" — writes client_config.pending_connectors (text[]) + pending_connectors_noted_at; the monthly incremental scan re-runs scan_skipped_reason chains when a pending connector activates. On the HIGHEST VANISH-RISK list; full spec at pre_agent_build_checklist D-20 (pending connectors, ~line 189). Must not be lost. [AMENDED 2026-07-27] VERIFIED: `client_config` has `pending_connectors` (ARRAY) but NOT `pending_connectors_noted_at` — half-built. *(checked-against: live DB query · 2026-07-27)*
- **BT-15 — Evidence Stack assembly for a fired alert.** Build the four layers defined in the operating charter: (1) WHAT happened, stated plainly, no jargon; (2) the RAW NUMBERS the founder can cross-check in 30 seconds in the source platform; (3) HISTORICAL PRECEDENT — a specific date and outcome from THIS brand's own data, never an industry benchmark; (4) ranked CORRECTIVE ACTIONS with projected impact and a confidence score. If layer 3 is unavailable through thin history, the alert must SAY SO rather than substitute a generic benchmark. This is the entire substance of an alert and had no numbered item until 2026-07-24. [AMENDED 2026-07-27] DEPENDENCIES: Layer 0 reads `permanent_dq_limitations`, which has ZERO rows (CD-5). Layer 2 reads `alert_data_lineage`, which has ZERO rows against 177 alerts in `alert_log` (CD-8). Both must be resolved or the Evidence Stack ships with two empty layers.
- **BT-16 — [PILOT] G1 split into two firing conditions (G1a / G1b).** G1a = every variant of a product at zero with ad spend continuing; applies to ALL ad types including catalogue — if it fires on a catalogue ad the feed has failed, which is itself worth telling the founder within hours. G1b = SIZE-CURVE COLLAPSE: the product is nominally in stock but the variants carrying the majority of its own historical demand are at zero; threshold is brand- and product-relative (share of that product's own trailing unit sales now unavailable), no hardcoded number. WHY: the 2026-06-13 pilot scoping narrowed G1 to single-product-destination ads on the premise that "catalog ads self-suppress OOS". Meta's own documentation states only ONE variant of each item displays in ads, and that it may substitute an available variant when the viewed one is unavailable — so catalogue ads suppress the sold-out ITEM but keep advertising the PRODUCT and keep spending. Catalogue accuracy also runs 92-97% on the native channel app with a 15-minute-to-24-hour sync delay, so the premise is directionally right but treated as more reliable than it is. DATA STATUS *(checked-against: live DB query · 2026-07-27)*: the load-bearing join is SOUND — 137,006 of 137,006 order line items match a variant row, on both the staging and raw sources. Detection is therefore buildable and testable on synthetic data TODAY using product_id + variant + quantity + inventory_quantity; only the human-readable SIZE LABEL is blocked, on BT-20. FIRST TASK: verify `variant.inventory_item_id` joins to inventory_levels with coverage, and check whether `variant.title` / `display_name` retained any option text. OPEN SUB-ITEM: G1a's action is clean (pause the ad set); G1b's is a judgement (exclude from feed / restock / accept) and its Layer 4 must offer ranked options, not one instruction. Adjacent to BT-4 — BT-16 defines WHAT fires, BT-4 wires it into Agent A. Doc changes carried at A34.
- **BT-17 — [PILOT] Backfill variant-level inventory history.** Route: `shopifyqlQuery` in the GraphQL Admin API against `inventory_adjustment_history` — pointed to by a Shopify staff member in the same community thread whose headline answer is "no historical inventory API". Retention ceiling ~180 days; older data is not retained. FIRST TASK: verify availability, the scope required, and the ceiling, on a development store — **UNVERIFIED until a Shopify Partner account exists (FC-5)**. WHY THIS IS MORE THAN A 30-DAY SAVING: `technical_architecture.md:1158` says the B-9 inventory columns "populate as daily Airbyte syncs accumulate", but only TWO DISTINCT EXTRACTION DATES exist, eight days apart (2026-05-31, 2026-06-08) — daily syncs are not running, so waiting does not fix this *(checked-against: live DB query · 2026-07-27)*. G1's `days_since_sku_went_oos` and Evidence Stack Layer 3 both need a stock history. DO NOT substitute order-line reconstruction: it misses restocks, manual corrections, damage write-offs, inter-location transfers and returns-to-stock — exactly the movements that decide when a variant crossed zero, which is the only moment G1 cares about. Related to D-1 (a mart joining inventory to spend) but distinct: BT-17 is about having a history at all.
- **BT-18 — `top_sku_inventory_pct` is NULL on a CURRENT mart — B-4 alias map unwired or not working.** VERIFIED: 0 non-null of 730 rows in `client_azure_co_marts.mart_causal_chain_daily` *(checked-against: live DB query · 2026-07-27)*. The mart is NOT stale — dbt target artifacts show it was last materialised 2026-06-26, five weeks after the B-4 alias map (2026-05-22), and it physically carries all seven B-9 columns. The other five inventory-derived columns each carry 1 non-null of 730; `back_in_stock_waitlist_count` carries 730 of 730 (Klaviyo-derived per product_strategy G4 and checklist D-2 — INFERRED from the docs, not verified in the mart SQL). So the single-snapshot inventory feed explains the SPARSE columns; it does NOT explain this one, which is null even on the date the others populate. Therefore a BUILD defect, not only a doc fix. DOC HALF: `technical_architecture.md:1160` AND `:2095` both state "RESOLVED by B-4 alias map (2026-05-22). Column now populated" — both wrong; carried at A35. Overlaps BT-6 (debt/plumbing) — cross-referenced so it is not built twice. UNVERIFIED and not relied on here: that the five sparse columns are non-null specifically on the single inventory snapshot date; it fits but was never tested.
- **BT-19 — Targeted sweep of `pre_agent_build_checklist.md`.** Read three sections — "DEFERRED — NOT BLOCKING ANY AGENT", "PRE-AGENT C/D GAPS", "PRE-FIRST-CLIENT GAPS" — asking one question per row: does this still block a pilot alert? ~45 rows, roughly half already COMPLETE. WHY: the checklist is well maintained, but its Deferred section defers items to "Agent B design", a milestone the post-pivot pilot plan does not contain. D-1 was found there, described as "G1 full validation needs SKU-level join between inventory and paid spend" — parked behind a gate that no longer exists. Fix in place while there: D-6 (its blocker B-4 is COMPLETE), CD-13 (NLQ marked "design in Agent D session"; moved post-pilot 2026-07-24), D-22 (conflict, below). 1-2 hours. **Treat this register as probably-incomplete until this runs.**
- **BT-20 — Variant seed defects (three findings, one root cause).** The seed cannot exercise the cases the pilot alerts exist to catch *(checked-against: live DB query · 2026-07-27)*. (1) NO VARIANT CARRIES SIZE — `option1`, `option2` and `option3` are entirely NULL across `shopify_product_variants`, and SKUs repeat ~5 times per style with no size discriminator (25-row sample, not a full scan), so five variants per product exist but are undifferentiated. Blocks the size LABEL in BT-16, not its detection logic. (2) `inventory_policy` is uniformly DENY — the column reads correctly, but the seed never produces "continue", which is the Shopify setting that makes a product report as available at zero stock and is precisely the catalogue-suppression failure G1b targets. (3) At least one malformed SKU carries an embedded newline. Root cause is one script; fixable in one sitting. NOTE: `option1/2/3` are standard Shopify variant fields that a real store populates — this is a seed gap, not a platform limitation.

---

## PROMOTED FROM pre_agent_build_checklist.md (2026-07-27)

These keep their ORIGINAL checklist codes deliberately — the code is the searchable link back to the checklist, and renumbering would break that thread. Seven are live; four were closed by the 2026-07-27 database verification and are recorded below so BT-19's sweep does not re-raise them.

- **D-1 — `mart_inventory_spend_daily` (SKU-level).** VERIFIED ABSENT in every schema *(checked-against: live DB query · 2026-07-27)*. Checklist note: "G1 full validation needs SKU-level join between inventory and paid spend. Build after Agent B query pattern defined." Deferred to a milestone the pilot plan does not contain. Feeds BT-16.
- **CD-5 — `permanent_dq_limitations` rows seeded.** VERIFIED: table PRESENT in `public`, 0 rows — exactly as the checklist states *(checked-against: live DB query · 2026-07-27)*. Required for Evidence Stack Layer 0. BT-15 builds the Evidence Stack against an empty table unless this is seeded first. The seven rows are already specified in `technical_architecture.md`:2332-2340.
- **CD-8 — `alert_data_lineage` has ZERO ROWS against 177 alerts.** Original question CLOSES — the table exists in `client_azure_co`. But `alert_log` holds 177 rows and `alert_data_lineage` holds 0, so 177 alerts were produced and none wrote lineage *(checked-against: live DB query · 2026-07-27)*. That table is what Evidence Stack Layer 2 reads — the raw numbers a founder cross-checks in 30 seconds. **BT-15 has a dependency nobody had recorded.**
- **CD-14 — Agent D display rule: `avg_days_inventory_on_hand` = 999.** Never show the raw value; display "zero-velocity SKU — likely overstock or discontinued". Applies across all G-series Evidence Stack output, so it touches G1.
- **D-10 — `business_model_type` column in `client_config`.** VERIFIED ABSENT from ~160 columns *(checked-against: live DB query · 2026-07-27)*. The onboarding answer lives only in `onboarding_log.json`. Needs ALTER TABLE plus an `onboarding_flow.py` update before a real brand onboards.
- **B-12 — Real-data hardening, six items.** (1) +/-1 day tolerance on all cross-source date joins — this is G1's timing fix; (2) `data_lag_hours` per source in `client_config`; (3) attribution-window permanent DQ entries; (4) PMAX_DIAGNOSTIC_BLOCKED entry; (5) Google Ads rounding disclosure in Evidence Stack Layer 0 for G-series spend alerts; (6) `campaign_type` not `campaign_name` in all founder-facing text — real accounts rename campaigns constantly. Checklist marks this "must complete before first beta client onboards".
- **D-22 — Gorgias NLP classifier gate CONFLICTS with BT-9.** The checklist says do not build until discovery confirms tagging is unreliable across 3+ beta clients. BT-9 says finalise the parser process before the pilot, and the committed 2026-06-04 direction is to parse ticket TEXT rather than trust tags. The checklist is holding a gate the register has already walked through. **Needs one ruling, then D-22 is updated in place.**

*Closed by the 2026-07-27 verification, do not re-raise: **CD-6** (`suppression_log` present, 29 rows) · **CD-7** (`brand_event_calendar` present, 116 rows) · **CD-9** (`dq_metric_scores` present, 42 rows) · **D-6** (G1 ad-set-to-SKU mapping — its blocker B-4 is COMPLETE 2026-05-22, and `technical_architecture.md`:2301-2313 treats product-level spend attribution as a permanent Phase-1 limitation with a decided spend-only fallback; not remaining work).*

---

## FIRST-CONNECT ACTIONS (the day the first brand connects)

- **FC-1 — Build `validate_sync.py`** (R10) — founder-ruled first-connect; reword D-F(2).
- **FC-2 — Repoint C8/returns to Shopify-native** (J-1) + prove native ingestion. *(tech-arch:1461-1463; D-D.)*
- **FC-3 — C1 recalibration** (CD-10; manual p90 interim).
- **FC-4 — Persistent paid Supabase + keep-alive.**
- **FC-5 — Start connector access for ALL platforms, prioritized by lead time:**
  - First (long lead, start today): Shopify CUSTOM DISTRIBUTION + OAuth (read_all_orders approval is the long-lead item; protected-data review NOT required — aligns with pilot_scope §6, which has pilot precedence); Google Ads developer token. *(checked-against: pilot_scope.md · 2026-07-24)*
  - Second (pilot-fired, now): Meta API; Gorgias API key; Loop Returns API key.
  - Third (soon after): TikTok API; Klaviyo API key; Sentry (only brands that have it).
  - Plus (long-lead, non-connector): entity registration.
- **FC-6 — [promoted from CD-2] Build the brand's event calendar from their own history** (engine is design-only, unbuilt); fix `historical_pattern_scan.py` stale "zero rows" comments (lines 31, 621-624, 811, 1238).
- **FC-7 — Validate parser labels on REAL brand data** (pairs with BT-9).

---

## PRE-PILOT GATE

- **PG-1 — D-F 7-condition HARD gate** → `state_2026-06-23_prepilot-hardening.md:172-180`. Not yet canonical.
  - Status: (1) DONE · (2) REWORD-per-ruling · (3) DONE · (4) PARTIAL · (5) OPEN · (6) OPEN · (7) OPEN.
  - Commitments: stays a hard gate; documented here; DEEP REVIEW of all 7 conditions before pilot.
- **PG-2 — Legal counsel pass (before first connect).** One lawyer review before the FIRST real brand connects. Scope: the data agreement with the merchant — which pilot_scope §6 lists among the protected-data requirements that apply in full even though no Shopify review is required for a custom-distribution app — plus the site's legal pages. A gate before first connect, not a build task. Had no numbered item until 2026-07-24.

---

## HOUSEKEEPING

- **HK-1 — Untracked/uncommitted working tree.** 6 untracked continuity files + `slack_bot/` untracked (now stale since delivery = email — flag, don't delete); `seed_meta.py` + `onboarding_flow.py` modified-uncommitted.
- **HK-2 — OneDrive/.git corruption risk** (accepted; the repo is the source of truth).
- **HK-3 — [CLOSED]** stale roster counts 41/56/37 — folded into the doc-sync count fix (CAO-1 + PAC-1 resolved).
- **HK-4 — Founder runs all commits and pushes** (through Claude Code; explicit-path staging only).
- **HK-5 — [watch during pilot] Cold-start visibility gap.** Removing the query feature from the pilot (now post-pilot, product_strategy §3C) means the product may be invisible to the founder between connect and first alert. The connect-day email is the chosen mitigation. An observation to MONITOR during the pilot — not an open question. Source: rationale preserved in product_strategy §3C.
- **HK-6 — [verify before commit] Straight quotes only, never curly/smart quotes.** This file family uses STRAIGHT ASCII quotes throughout. Two drifts occurred 2026-07-24: an editor auto-formatted straight quotes to curly on a committed file, and a regenerated copy did the reverse. Because every edit in this process is located by EXACT TEXT MATCH, a silent straight-to-curly (or curly-to-straight) substitution produces false not-found results that look like missing edits. Verify quote style before every commit.
- **HK-7 — [log, do not chase] Schema drift.** `CLAUDE.md` states "No other schemas exist yet" after naming public / client_azure_co / client_azure_co_staging / client_azure_co_marts. Four more exist *(checked-against: live DB query · 2026-07-27)*: `client_azure_co_client_azure_co_marts` (doubled prefix, likely a compounded `client_schema` var), `public_marts` (holds a STALE 51-column copy of `mart_causal_chain_daily` carrying none of the B-9 columns; the live 85-column copy is in `client_azure_co_marts`), and `dbt_anupam313_marts` / `dbt_anupam313_staging` (dev). Also: `brand_event_calendar` exists in two schemas (client_azure_co 116 rows, public 0 rows), and near-empty 1-row duplicates of `stg_shopify_inventory_levels` and `stg_shopify_product_variants` sit in `client_azure_co` alongside the real 626-row staging view in `client_azure_co_staging`. NOT cross-CLIENT — one client's data duplicated across dev and prod-ish schemas, so RULE 2 is not violated. The CLAUDE.md sentence needs correcting (A35 batch).

---

## DOC-HYGIENE

- **DH-1 — [RESOLVED 2026-07-02] Stale charter / onboarding-brief.** The pasted IDENTITY brief described the pre-pivot product (old five alerts / Slack / $299 / 20 clients / a non-existent `seed_design_decisions.md`). **Resolution:** its stale FACTS are now explicitly marked superseded, item-by-item with committed sources, in `charter_facts_superseded_2026-07-02.md`. The charter's STANCE and PROTOCOLS remain fully in force. Two embedded scope questions were flagged for founder ruling (see that record: the "20 beta clients" milestone, and adopting a corrected charter to stop re-injection). **Follow-on [CLOSED 2026-07-24]:** the Blueprint staleness check is done — `Profit_Sentinel_Blueprint_v9.docx` exists and is reconciled (July 2026) with an explicit pilot-vs-post-pilot scope box, the five-alert pilot set named, and pricing marked indicative. Closure recorded in charter_facts_superseded_2026-07-02.md. *(checked-against: charter_facts_superseded_2026-07-02.md · 2026-07-24)*
- **DH-2 — Blueprint influencer competitive claim is overstated.** `Profit_Sentinel_Blueprint_v9.docx` §7.4 states that return-adjusted influencer ROI by creator is "not built by PS or any competitor... neither side has it yet. First mover takes this." Web research 2026-07-27: Triple Whale's **Affluencer Hub** ships creator-level revenue, creator-level cost including one-off and recurring expenses, and per-creator ROAS, with discount-code and tracking-link attribution and historical order backfill. GRIN, Aspire, Upfluence and CreatorIQ all carry per-creator sales attribution. NOT found anywhere: netting returns at creator level from the brand's own refund data — Triple Whale documents that it does not track refunds from third-party returns apps such as Loop. So the gap is real but NARROWER than claimed, and it is specifically the returns leg. ALSO RECLASSIFY from MOAT to WEDGE: the incumbent already holds the creator infrastructure, so "minus refunds on those orders" is a small step for them, not a long lead. Carried at A35.

---

## CONDITIONAL (re-surface only if deferred work is pulled forward)

- **CD-1 — Segment-boundary calibration** (E-series/segments, Phase-2).
- **CD-3 — Klaviyo 0.65 open-rate default at onboarding** (Klaviyo/E-series, Phase-2).
- *(CD-2 promoted to FC-6.)*
- **CD-4 — [PARKED until code pass] Confirm Agent D output can be cleanly intercepted before sending.** The compose → hold → verdict → send loop (BT-13) assumes Agent D's output can be held before delivery; verify in the codebase. Park until the code pass.

---

## HELD (deferred by decision 2026-07-27 — recorded per save_protocol Check 11)

- **HELD-1 — FC-4 reclassification, first-connect to Block A.** The Supabase project auto-paused on the free tier and blocked a Block A verification session on 2026-07-27. FC-4's reasoning ("real brand data cannot sit on a database that sleeps") is right but incomplete — synthetic data cannot either, if anything is to be verified against it. HELD at founder instruction as tactical; the reasoning is recorded so it is not rediscovered.
- **HELD-2 — Placement of the Step 0 / Step 0b verification SQL in the repo.** Both scripts are validated and currently live outside the repo. Worth keeping: re-runnable at first connect and after any rebuild, adjacent to FC-1. Path must come from CLAUDE.md's FILE LOCATIONS section, not a guess. HELD at founder instruction.
- **HELD-3 — Supabase paid-plan trigger. [OPEN — no other home]** Staying on free is deliberate and reasonable: pausing costs least precisely when not building, and converting takes minutes. But the trigger is NOT "when I have some official data" — `pilot_scope.md` §6 lists **encrypted backups** among the protected-data requirements that apply in full before a real brand connects, and the free tier has ZERO days of backup retention, so it cannot satisfy a commitment already made. Upgrade before first connect, for backups, not for pausing. REVISIT IMMEDIATELY IF: BT-19 or the reproducibility check shows schema DDL or RLS policies are not in the repo — in that case a pause running to deletion costs real reconstruction, not a rebuild script. Attach to FC-4 when FC-4 is next touched.

---

## HIGHEST VANISH-RISK (this register is their only home)

These have **no canonical home** anywhere else — if they leave this register, they are lost: **OQ-2, OQ-8, OQ-9, OQ-1** (abnormality method), **OQ-13** (graduation rule), **OQ-14** (digest content), **D-20** (now BT-14), **PG-1** (the whole D-F gate). DH-1 was in this set; it is now resolved. Added 2026-07-27: **OQ-15** and **OQ-16** (no canonical home), and **HELD-3** (the Supabase paid-plan trigger, which exists in no file at all).
